import { Bell, createIcons, CreditCard, House, Repeat, Search, Settings } from "lucide";

function init() {
  createIcons({
    icons: {
      Bell,
      Search,
      House,
      CreditCard,
      Repeat,
      Settings,
    }
  })
}

init();