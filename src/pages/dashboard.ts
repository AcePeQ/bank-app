import { Bell, createIcons, CreditCard, House, LogOut, Repeat, Search, Settings } from "lucide";

function init() {
  createIcons({
    icons: {
      Bell,
      Search,
      House,
      CreditCard,
      Repeat,
      Settings,
      LogOut
    }
  })
}

init();